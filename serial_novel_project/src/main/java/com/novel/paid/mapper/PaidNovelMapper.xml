<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http:/mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.novel.paid.mapper.PaidNovelMapper">
	
		<select id="selectList" resultType="com.novel.paid.domain.PaidVO">
		SELECT n.novel_title , n.novel_num, p.paid_num FROM novel_tbl n INNER JOIN paid_tbl p ON n.novel_num = p.novel_num 
			WHERE n.novel_week = #{novelWeek} GROUP BY n.novel_title, n.novel_num, p.paid_num
		</select>
		
		<select id="selectWeekList" resultType="com.novel.paid.domain.PaidVO">
			SELECT * FROM novel_tbl WHERE novel_week = #{novelWeek}
		</select>
		
		<select id="selectDetail" resultType="com.novel.paid.domain.PaidVO">
			SELECT *
			 FROM paid_tbl p INNER JOIN 
			novel_tbl n ON p.novel_num = n.novel_num WHERE paid_num = #{paidNum} and p.novel_num = #{novelNum} 
		</select>
		
		<select id="detailCon" resultType="com.novel.paid.domain.PaidVO">
			SELECT paid_content1,paid_content2 FROM paid_tbl WHERE paid_num = #{paidNum}
		</select>
		
		<select id="selectPaidList" resultType="com.novel.paid.domain.PaidVO">
			SELECT * FROM paid_tbl WHERE novel_num = #{novelNum}
		</select>
		
	</mapper>
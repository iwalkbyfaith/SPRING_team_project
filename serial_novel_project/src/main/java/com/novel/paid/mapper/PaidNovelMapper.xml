<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http:/mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.novel.paid.mapper.PaidNovelMapper">
	
		<select id="getPaidList" resultType="com.novel.paid.domain.PaidVO">
			select * from paid_tbl ORDER BY paid_num DESC
		</select>
		
		
		<select id="paidDetail" resultType="com.novel.paid.domain.PaidVO">
			select * from paid_tbl p left join novel_tbl n on p.novel_num = n.novel_num where p.novel_num = #{novel_num}
		</select>
		
		<insert id="insertPaid">
			INSERT INTO paid_tbl (paid_num, novel_num, paid_snum, paid_title, paid_content) values
                        (paid_num.nextval, #{novel_num}, #{paid_snum}, #{paid_title}, #{paid_content})
		</insert>
	</mapper>